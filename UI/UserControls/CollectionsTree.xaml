<UserControl x:Class="osu_collection_manager.UI.UserControls.CollectionsTree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:osu_collection_manager.UI.UserControls"
             xmlns:models="clr-namespace:osu_collection_manager.UI.UserControls.Models"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <TreeView x:Name="test" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ItemsSource="{Binding}">
        <TreeView.Resources>
            <DataTemplate DataType="models:CollectionHolder">
                <CheckBox IsChecked="{Binding Selected}" Click="Checkbox_OnClick">
                    <TextBlock Text="{Binding Data.Name}"></TextBlock>
                </CheckBox>
            </DataTemplate>
        </TreeView.Resources>
    </TreeView>
</UserControl>
